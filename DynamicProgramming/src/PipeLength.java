import java.util.*;
public class PipeLength {
	
	public static void main(String args[])
	{
		int[] arr={6,1,2,3,4};
		PipeLength p=new PipeLength();
		p.findLength(arr,5);
	}
	
	public int findLength(int[] arr, int l)
	{
		int max_length=0;
		int no_of_cases=(int)Math.pow(3.0, (double)l);
		int[] cases=new int[no_of_cases];
		
		
		return max_length;
	}
}
